previews:
  generation: automatic

services:
  - type: web
    name: django-ai-chat
    env: python
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn healthcare_website.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: healthcare.settings
